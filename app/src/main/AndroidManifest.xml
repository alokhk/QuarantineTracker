<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ak86.quarantinetracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CoronaActivity"></activity>
        <activity android:name=".EditDeleteBlockActivity" />
        <activity android:name=".BarrackTypeActivity" />
        <activity android:name=".BlockDetailActivity" />
        <activity
            android:name=".PasswordResetActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".CreateNewUser"
            android:screenOrientation="portrait" />
        <activity
            android:name=".AddBlockActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".LandingActivity"
            android:label="@string/title_activity_block" />
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>